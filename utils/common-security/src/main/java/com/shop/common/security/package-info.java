/**
 * Resource-server security: OAuth2 / Keycloak JWT validation, CORS auto-config,
 * JWT claim helpers, and a default {@code SecurityFilterChain} that services can
 * either accept as-is or override per-endpoint.
 */
package com.shop.common.security;
