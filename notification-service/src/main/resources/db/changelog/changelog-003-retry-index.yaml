databaseChangeLog:
  - changeSet:
      id: 003-notification-retry-index
      author: shop-platform
      changes:
        - sql:
            sql: CREATE INDEX idx_notifications_status_next_retry ON notifications (status, next_retry_at);
